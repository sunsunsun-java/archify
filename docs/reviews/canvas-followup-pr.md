# PR #531 后续：画布交互与独立 HTML 优化

## 基线与范围

PR [#531](https://github.com/tt-a1i/archify/pull/531) 已于 2026-09-23 合入 `labs/infinite-canvas`，合并提交为 `babe92417df235078cd624539ff229546e5fbb0c`。该提交的代码树与原开发起点 `018378126c5d09085ef79afcd28434d388e8a2bf` 完全相同。本分支已快进到该合并提交，后续 PR 只增加下述工作，不重复 #531 的提交。

- 分级世界坐标点阵、较轻的画布阴影、稳定工具栏和固定图例；作者 SVG 与导出语义保持。
- 透镜激活保留已有镜头，避免选择数据库与安全后意外回到 100% 而裁切底部节点。
- 路径、地图、透镜六向切换互斥；保留镜头、正确清理旧状态和分享 hash，保留不相关 URL 锚点。
- 生成阶段压缩 JS/CSS，模板由 843,544 B 降为 651,529 B（减少 22.8%）；功能、字体、语言字典及离线交付能力保留。

## 验证证据与复用边界

[最终优化记录](html-size-optimization-20260923.md)及其[证据 JSON](html-size-optimization-evidence.json)覆盖以上合并后的实现。准备 PR 时重新核对其中 20 个候选源码哈希及 54 个候选测试哈希，全部匹配。因此复用该工作树身份下的完整门禁：Node 2074 通过、76 条件跳过、0 失败；公共浏览器 393/393、0 跳过；额外架构取景 46/46；真实 WebM 与站点语言集成通过。没有将 #531 的 CI 结果冒充本次分支结果。

本次准备额外复核：生成与体积合同 278/278；模板 freshness；`git diff --check`。压缩只改变交付代码排版，六份同输入样本的 SVG 与章节/证据 JSON 保持完全一致。Node 22 包外 smoke 已再次通过；重新构建的 84 文件 ZIP 与待提交 `archify.zip` 逐字节一致。

本地结果来自 macOS、Node 22、Chrome；其他 OS/Node 的远程 CI 尚待本 PR 执行。历史阶段报告中的“尚未提交/等待 #531”描述其记录时间点，由本文提供后续状态，不改写历史证据哈希或将历史失败隐藏。

## 视觉与消融

公共五类图、两主题、预设与语言对照链接见[画布实施记录](canvas-visual-polish-goal-20260923.md)。HTML 压缩前后 28 组对照最大几何差异小于 0.001 CSS px；少量文字栅格差异经检查未发现可辨退化，未声称全部逐像素一致。

实际移除冗余网格跨层标记与无消费者的 `sourceRoot` 参数后原验收仍通过。去掉图例缓存会增加 1725 次 DOM 写入；去掉 Lens 镜头保留会重新出现 144 px 越界；去掉直接类型选择的 Map 清理会破坏互斥。这三项必要行为均恢复保留。详细记录分别见[画布](canvas-visual-polish-goal-20260923.md)、[镜头](lens-preserve-camera-20260923.md)、[互斥](navigation-exclusive-20260923.md)、[体积](html-size-optimization-20260923.md)。

## 交付范围

模板、两套示例、Gallery、Checkout 对照及回执、README 动图与来源、Node 22 ZIP 同步更新。Guide/Start 作者输入未改变，保持原产物。Maka 私有输入、源码图和本地截图不随 PR 发布。没有 schema、发布版本、live Skill 安装或数据迁移变更。
